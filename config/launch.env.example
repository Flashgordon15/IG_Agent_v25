# Copy to config/credentials/launch.env (gitignored) for headless Flight Deck launch.
# Used when double-clicking IG Agent Flight Deck.app without a terminal attached.
AGENT_LAUNCH_PASS=your-ig-password-here
